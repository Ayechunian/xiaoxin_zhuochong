# 本地提醒中心

右键桌宠并选择 `提醒中心…`，会打开一个独立的交互页面。右键菜单只保留这一项入口，新增和管理操作都在页面内完成。

## 添加提醒

在 `添加提醒` 页面填写内容，然后选择：

- 10 分钟后
- 30 分钟后
- 1 小时后
- 下一个 18:00
- 自定义日期和时间

添加成功后，页面会显示确认信息。提醒保存在 `data/reminders.json`，关闭或重启桌宠后仍然有效。

## 到点提醒

到点后桌宠使用现有点击动作，并持续显示浅暖黄色气泡。提醒内容采用加粗深色字体，下面显示“点击我确认完成”。气泡不会自动消失，也不会被普通对话、整点问候或专注计时覆盖。

单击桌宠会将当前提醒标记为完成。若多条提醒同时到期，确认一条后会继续显示下一条。

## 管理提醒

`管理提醒` 页面按时间排列所有待处理项目，可以：

- 标记完成
- 延后 10 分钟
- 删除

这些操作全部位于提醒中心页面，不会扩展右键菜单。

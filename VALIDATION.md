# 本机验证记录

2026-09-12，Windows x64。

- Python 3.13.9；PySide6-Essentials / shiboken6 6.9.2（本机 Anaconda 环境下 6.11.2 的 Qt DLL 导入失败，降至兼容版本后正常）。
- `check.py`：26 张分镜透明帧、左右各 8 帧成套行走、6 帧转身衔接、4 帧待机、置顶和透明属性、点击、拖动、睡眠、缩放和屏幕边缘互动均通过。
- 行走配置：模拟心跳 20ms，8 帧完整重建循环按 `100/110/130/180/100/180/110/90ms` 非均匀换帧，经过/交叉相位使用较长停留，完整周期 1000ms，周期位移 48px；所有行走帧透明底线均为 y=186。
- 待机眨眼：默认睁眼，随机间隔 4–8 秒，单次闭眼 95ms，约 1/3 概率连续眨两下。
- `check_edges.py`：四边贴边动作、拖出恢复、透明裁切、缩放锚点和角落选择均通过。
- `check_reminders.py`：提醒持久化、到期提示、完成、延后、删除和提醒中心页面均通过。
- `main.py --smoke-test`：Qt 窗口启动和退出成功，退出码 0。
- `main.py --export-assets`：按当前帧数导出状态 PNG。
- `start.bat`：已实际启动 `F:\\xiaoxin_zhuochong\\main.py`，桌宠进程运行正常。

自动交互检查已通过；未人工验证所有多显示器与缩放组合。
